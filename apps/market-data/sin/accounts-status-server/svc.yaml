apiVersion: v1
kind: Service
metadata:
  name: accounts-status-server
  namespace: accounts-status-server
  labels:
    app: accounts-status-server
spec:
  type: ClusterIP
  selector:
    app: accounts-status-server
  ports:
    - name: in
      protocol: TCP
      port: 5000
      targetPort: 5000
